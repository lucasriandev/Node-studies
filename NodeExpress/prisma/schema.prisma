generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Mensagem {
  id        Int      @id @default(autoincrement())
  texto     String
  idade     Int      // <-- Nova coluna adicionada na planta!
  criadoEm  DateTime @default(now())
}