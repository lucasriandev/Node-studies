generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Mensagem {
  id        Int      @id @default(autoincrement())
  texto     String
  criadoEm  DateTime @default(now())
}