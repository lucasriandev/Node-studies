generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model cadastros {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  senha     Int
  createdAt DateTime @default(now())
}
